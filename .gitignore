# byte-compiled / cache
__pycache__/
*.pyc

# large data artifacts tracked via Git LFS instead
# (see .gitattributes -- these patterns stay in git as pointer files)

# OS/editor cruft
.DS_Store
.ipynb_checkpoints/

# large source/derived data artifacts -- too big for GitHub's 100MB limit
# and for the free LFS storage quota; kept on disk, not versioned
dialogueText_196.csv
df_with_sentiment.pkl

# transformer-branch local environment -- repo-local conda-prefix env,
# never committed (isolation rules: env stays local to this clone)
.venv/
requirements.lock.txt.tmp
