# Extraction environment -- matches the environment that WROTE
# df_with_sentiment.pkl, so deserialization compatibility is handled here
# and never leaks into the model runtime (requirements.txt).
#
# pandas pickle compatibility across major versions is not a reliable
# archival contract, so these are matched to the writer rather than chosen
# for convenience. Verified read-only from the base Anaconda env on
# 2026-09-04:
#     pyarrow 19.0.0
#     pandas  3.0.1
#
# This env is used ONLY by scripts/build_real_sample.py, to load the source
# pickle once and emit data/real_sample.csv. The output is CSV precisely so
# the transformer environment can read it with plain pandas 2.2.3 and needs
# neither pyarrow nor pandas 3.x.
--index-url https://pypi.org/simple

pandas==3.0.1
pyarrow==19.0.0

# numpy pinned to the writer's IMPORTED runtime version, not its package
# metadata. The read-only check on 2026-09-04 returned conflicting values:
#     conda list      -> numpy 2.4.2
#     imported version-> numpy 2.4.6
# The environment that actually wrote the pickle behaves as 2.4.6, so that
# is what is pinned; the conda metadata is stale or was shadowed by a
# pip-installed upgrade. Consistent with the pickle stream, which
# references numpy._core.numeric._frombuffer (a numpy 2.x path).
#
# If deserialization raises a numpy-specific error despite this pin, the
# metadata/runtime split is the first thing to re-check.
numpy==2.4.6
